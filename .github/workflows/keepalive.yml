name: Keepalive
on:
  schedule:
    - cron: "*/10 * * * *"  # 10분마다
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://chem-pre-lab-web.onrender.com/api/keepalive
