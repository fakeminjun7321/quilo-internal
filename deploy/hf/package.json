{
  "name": "pdf-translate",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "독립 PDF 통번역 서버 (HF Spaces Docker)",
  "scripts": {
    "start": "node translate-server.js",
    "postinstall": "python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt && .venv/bin/python scripts/check-python-runtime.py && bash scripts/install-tectonic.sh && command -v tectonic >/dev/null && tectonic --version"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "image-size": "^1.2.0",
    "multer": "^2.0.0",
    "sharp": "^0.33.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
